import { ReportConstructorPage } from "./features/report-constructor";

/** Authenticated app surface — the Report Constructor wizard. */
export default function App() {
    return <ReportConstructorPage />;
}
